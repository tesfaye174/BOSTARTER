// JS scaffold for dash.php

(function () {
    'use strict';

    function init() {
        // dashboard specific code
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
