// JS scaffold for statistiche.php

(function () {
    'use strict';

    function init() {
        // statistics charts initialisation
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
