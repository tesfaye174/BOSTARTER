// JS scaffold for index.php

(function () {
    'use strict';

    function init() {
        // landing page interactions
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
