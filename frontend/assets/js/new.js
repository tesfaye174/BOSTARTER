// JS scaffold for new.php (project creation)

(function () {
    'use strict';

    function init() {
        // project creation helpers, client-side validation
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
