// JS scaffold for view.php

(function () {
    'use strict';

    function init() {
        // project view interactions (comments, support)
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', init);
    } else {
        init();
    }
})();
